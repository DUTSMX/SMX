 <style>
        .margin-0{margin:0px}
        .logo{height:50px;padding:5px}
        .center-vertical{vertical-align: middle}
        .margin-40{margin-left:10%}
    </style>
<nav class="navbar navbar-default margin-0" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <img src="/images/logo.png" class="logo" onclick="home()"/>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-left margin-40">
                <li><a href="/">返回首页</a></li>
                <li><a href="#">关于我们</a></li>
                <li><a href="#">课程介绍</a></li>
                <li><a href="#">视频平台</a></li>
                <li><a href="#">联系我们</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a id="person" onclick="loginDialog()">登录</a></li>
            </ul>
            <form class="navbar-form navbar-right" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="请输入搜索内容">
                    <button type="button" class="btn btn-success">搜索</button>
                </div>
            </form>
        </div>
    </div>
</nav>
<script>
    userName = '<%=session.userName%>';
    var userId = '<%=session.userId%>';
    if(userName.length != 0){
        document.getElementById("person").innerHTML = userName
    }
    function home() {
        if (userName.length == 0) {
            document.getElementById("person").innerHTML="登录";
            location.href="/";
        }
        else {
            location.href="/";
        }
    }
    function logout(){
        $.post('/user/logout',{},function () {
            userName = '<%=session.userName%>';
            console.log("userName lenght:" + userName.length);
            location.href="/";
        })
    }
</script>